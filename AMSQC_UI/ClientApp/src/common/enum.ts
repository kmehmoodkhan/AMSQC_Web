export enum QuoteSteps {
    GetQuoteDetail = 1,
    SubmitQuote = 2,
}

export enum RequestStatus {
    Failure = 0,
    Success = 1,
}

export enum SubletCompletionStatus {
    Yes = 1,
    No = 2,
    NA = 3,
}
