export type QuoteDetails = {
    company: string;
    model: string;
    color: string;
    registration: string;
};

export type Error = {
    title: string;
    description: string;
    type: string;
};
